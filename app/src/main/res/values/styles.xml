<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/actionbar_bg</item>
        <!-- Toolbar颜色 -->
        <item name="colorPrimary">@color/actionbar_bg</item>
        <item name="toolbarStyle">@style/toolbar_style</item>
    </style>

    <style name="toolbar_style" parent="ThemeOverlay.AppCompat.Dark">
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/actionbar_bg</item>
        <!-- Toolbar颜色 -->
        <item name="colorPrimary">@color/actionbar_bg</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="navigationIcon">@drawable/ic_action_back</item>
    </style>

</resources>
